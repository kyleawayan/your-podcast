# Copy this file to values-secret.yaml and fill in real values.
# values-secret.yaml is gitignored and will NOT be committed.
#
# Usage: ./deploy.sh --values helm/values-secret.yaml
secret:
  geminiApiKey: "your-gemini-api-key"
  ttsBackend: "google_cloud" # or "elevenlabs"
  elevenlabsApiKey: "your-elevenlabs-api-key"
  googleCloudVoice1: "Kore"
  googleCloudVoice2: "Charon"
  googleCloudModel: "gemini-2.5-flash-preview-tts"

# Subreddits to fetch and podcast configs per user.
# "user" is just a name to separate episodes (see: your-podcast generate --user).
# fetchSubreddits should include all subreddits across all users.
overnightJobs:
  fetchSubreddits:
    - python
    - javascript
    - rust
  podcasts:
    - user: alice
      duration: 30
      subreddits:
        - python
        - javascript
    - user: bob
      duration: 60
      subreddits:
        - rust
        - javascript

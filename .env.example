# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/your_podcast

# Podcast Generation (Podcastfy)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# TTS Backend: "elevenlabs", "macos", or "chatterbox"
# Use "macos" for free high-quality voices (macOS only, requires Premium voices installed)
# Use "chatterbox" for local neural TTS with voice cloning (requires ~10s reference audio)
TTS_BACKEND=elevenlabs

# --------------------------------
# VOICES
# Voice 1 is the host
# Voice 2 is the co-host

# ElevenLabs voice IDs (only used when TTS_BACKEND=elevenlabs)
# Find voices at: https://elevenlabs.io/voice-library
ELEVENLABS_VOICE_1=okH1aHncYRU2dc9TP3hV
ELEVENLABS_VOICE_2=WIX8boagHAO6uMUqxXLz

# macOS TTS voices (only used when TTS_BACKEND=macos)
# ---
# Install Premium voices:
# System Settings > Accessibility > Read & Speak > System Voice "i" > Voice (on newer macOS versions like Sequoia and later)
# OR
# System Settings > Accessibility > Spoken Content (on older versions like Sonoma)
# ---
# List available voices: say -v \?
# To preview voice: say -v "Voice Name" "This is a sample text."
# ---
# Recommended voices are the Premium ones. (Search "Premium" in the voice list in the System Settings menus above). Unfortunately, the Siri voices are not available to usage through the "say" command.
# Note: macOS TTS requires ffmpeg for MP3 export: brew install ffmpeg
MACOS_VOICE_1=Zoe (Premium)
MACOS_VOICE_2=Lee (Premium)

# Chatterbox TTS voices (only used when TTS_BACKEND=chatterbox)
# ---
# Chatterbox-Turbo is a local neural TTS model that clones voices from ~10 second audio samples.
# Place your reference audio files in ./data/voices/
# Supported formats: .wav, .mp3, .flac
# ---
# Note: Chatterbox requires PyTorch and runs on GPU (CUDA), Apple Silicon (MPS), or CPU
CHATTERBOX_VOICE_1=./data/voices/person1.wav
CHATTERBOX_VOICE_2=./data/voices/person2.wav
